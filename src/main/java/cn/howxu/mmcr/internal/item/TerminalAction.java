package cn.howxu.mmcr.internal.item;

/**
 * Server-authoritative actions requested by a terminal client.
 *
 * @author howxu <dev@howxu.cn>
 */
public enum TerminalAction {
    SET_INVENTORY_MODE,
    SET_STAGE,
    SET_LEVEL,
    SET_PREVIEW_ENABLED,
    SET_PREVIEW_LAYER,
    CHECK,
    BUILD,
    DEMOLISH
}
