# AGENTS.md

## 项目概览

- 项目名称：`ModularMachinery-Community-Refoxed`
- 项目类型：Minecraft / Java / Gradle Mod 项目
- 主要目标：移植reference/mmce项目(MC 1.12.2 Forge) 到当前项目 MC 26.1.2 NeoForge

## 工作原则

- 优先做最小正确改动，不进行无关重构。
- 当涉及大范围重构时，直接进行大范围重构
- 保留用户或其他协作者已有改动，不擅自回滚、不覆盖无关文件。
- 不添加用户未要求的功能、抽象层或配置项。
- 如果需求存在多种解释，先说明假设；关键不确定点应先询问。
- 减少测试的使用，测试只是辅助，先完成整体，再进行测试调整，而不是做一点测一点。
- 涉及NeoForge API相关的资源(如测试，注册，事件，模型，DataGen等)，不要先去反编译neforge，先到上层AE2看看有没有案例可以参考。
- 减少subagent使用。小范围，单需求使用3个subagent，一个注重写代码，一个负责写测试，一个负责debug和收尾。大范围，多需求可以在3的倍数上叠加。如果可以单agent直接做完更好。

## 代码风格

- 沿用当前代码库的命名、格式、包结构和设计习惯。
- 不要为类命名添加MMCR前缀。

## 修改流程

## 验证要求

- 修改 Java / Gradle 相关代码后，优先运行合适的 Gradle 任务，例如：
  - `./gradlew compileJava --no-daemon`
  - `./gradlew build --no-daemon`
  - 项目已有的测试或检查任务
- 不要在未验证的情况下声称“已修复”或“全部通过”。

## 依赖与构建

- 不随意升级 Gradle、Forge/Fabric、Minecraft或其他核心依赖版本。
- 新增依赖前先确认确有必要，并优先复用项目已有依赖。
- 不提交本地构建产物、缓存、日志或 IDE 私有文件。

## Minecraft Mod 注意事项

- 修改注册、事件、Mixin、数据生成或资源路径时，应检查对应的资源文件和运行期引用。
- 保持客户端/服务端逻辑边界清晰，避免把客户端专用类引入通用或服务端路径。
- 涉及存档、方块实体、网络包或配置格式时，注意向后兼容和迁移风险。
- 资源文件路径、语言键、模型、材质和配方应与代码引用保持一致。

## 沟通规范

- 默认使用简体中文回复。
- 说明问题时直接给出文件路径、关键位置和原因。
- 完成后简要列出：改了什么、如何验证、是否有遗留风险。
- 如果发现无关问题，可以简短提示，但不要擅自修复。

## 禁止事项

- 不伪造测试结果、构建结果或外部资料。
- 不在源码中写入临时调试代码后遗留。
- 不引入大范围架构改动来解决局部问题。
- 不覆盖许可证、作者信息或项目声明。
