# Factory Scheduler Design

## 线程运行时调度

- 索引 0 是永久保留且可执行的普通线程。
- tick 先推进活动线程，再按线程索引为闲置线程调度。
- 每个闲置线程先验证并尝试自己的 lastRecipe；失败后才进入普通候选配方搜索。
- 闲置线程会在当前容量内复用；仅在没有已分配的可复用线程且容量未满时追加线程。
- lastRecipe 缓存在机器 ID、结构版本或修改器快照版本变化时失效；输入或输出暂时不足只使本次快速启动失败。
