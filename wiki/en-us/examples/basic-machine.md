# Basic Machine

This example will show a minimal machine from structure declaration through recipe registration and in-game validation.

Keep the final example copyable and make each file path explicit so it can be compared with the `example/` directory in the repository.
