# Java API

This page will be the entry point for the public Java API, including machine builders, structure definitions, behavior callbacks, and registration events.

API details should follow the published signatures and include a runnable minimal example for each major entry point.
