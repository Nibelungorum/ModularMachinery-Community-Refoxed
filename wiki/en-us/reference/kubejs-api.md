# KubeJS API

This page will document MMCR's KubeJS builders and the lifecycle of startup, server, and client scripts.

The final reference should link each builder topic to a complete script example rather than listing methods without context.
